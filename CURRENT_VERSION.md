# Current Version

**Version**: v688

Replay-autopilot blocked-plan early stops now generate closeable verifiable rules for `blocked_plan_status_stops_replay` before evolution execution. The v688 repair also lets plan-contract validation target replay-autopilot production PowerShell control-plane scripts while keeping `scripts/tests` paths invalid as production carriers.
