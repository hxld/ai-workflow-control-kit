# Skill Feedback

> 放在 `<workspace>/.memory/skill-feedback.md`。
> 只记录“技能本身哪里不好用”，不要把代码 bug 混进来。

| 日期 | 技能 | 问题类型 | 现象 | 根因判断 | 建议修正 |
|------|------|----------|------|----------|----------|
| 04-16 | example-skill | 边界误判 | 把不该包含的工作项算进估算 | 未先切交付边界 | 增加边界检查规则 |

## 问题类型建议值

- `误触发`
- `漏触发`
- `边界误判`
- `输出失衡`
- `模板误用`
- `流程顺序问题`

## 使用规则

- 一条反馈只记录一个主要问题
- `现象` 写用户能观察到的表现
- `根因判断` 写为什么发生
- `建议修正` 写下一轮该如何改技能 / 文档 / 守卫规则
- 如果问题只是一次性噪声，不要为了凑数量硬记
