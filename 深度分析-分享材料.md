# AI Workflow Control Kit — 深度分析（更新版）

> 分享准备材料。所有数字均经仓库源码核查，可复现命令见附录。

---

## 一、TL;DR

**它是一个可迁移的 AI 编码"控制平面"：用技能路由、生命周期钩子和无人值守回放评测，把 AI 编码从"凭感觉对话"约束成可安装、可审查、可验证、可回放的工程流程。**

它不是 AI agent、不是模型、不是 IDE 插件——是在 Claude Code / Codex 等 AI 编码宿主外层加装的**控制层**，把工作流规则、触发条件、验证门禁和评估机制提前固化。

**成熟度判断（2026-06-14）：** Stage 1/2/2b/2c/3 全部完成。零硬编码路径、零业务命名耦合、全部 166 个测试可移植。canonical 层零业务耦合。**已处于可产品化状态。**

---

## 二、是什么 / 不是什么

| 是 | 不是 |
|----|------|
| AI 编码宿主外层的控制平面 | AI agent / 模型 / IDE 插件 |
| 可安装、可校验的技能+hook+规则包 | 某个业务项目的实现 |
| 自包含的回放评测引擎 | 运行态会话或私密状态 |
| 个人工程化 → 产品化的真实过渡样本 | 已完成的产品 |

**仓库有意不包含：** token、API key、会话日志、SQLite 状态、业务源码、oracle diff、`.env`。

---

## 三、为什么需要它

| 痛点 | 表现 |
|------|------|
| AI 对话零标准 | 每次会话靠临时对话定规则，回到"我该先干嘛" |
| 审查质量不稳定 | AI 提交质量依赖个体发挥，没有固定门禁 |
| 重做不知原因 | 事后无法还原"那次为什么走了那条路" |
| 多机器同步贵 | 换电脑要人工回忆技能/hook/配置写在哪 |
| 工作流无法演进 | 改了技能后没有评估手段，不知道改得好不好 |

核心思路：**把"应该怎么做"写成可复用控制规则，而不是每次靠 AI 自行判断。**

---

## 四、怎么做到的：四层控制

### 4.1 路由层 — workflow-router（入口技能）

决定所有请求分流，只做分流和门禁提示，不内联执行下游技能（Iron Law）。

**六字段冻结工作形态：** `MODE → SCOPE → CONTRACT → VERIFY → EXIT → SYNC`

**五种 Mode：**

| Mode | 场景 | 默认动作 |
|------|------|----------|
| `planned-dev` | 需求/设计/功能实现/多 surface | 需求冻结→设计→审批→TDD→收口 |
| `hotfix` | 有失败测试/堆栈/复现的小修 | 锁证据→RED→最小 diff→回归 |
| `debug-only` | 只排查/查询/给结论 | 证据锚点→假设表→信息缺口 |
| `review-only` | 只审查/比较 | findings→风险→验证建议 |
| `tooling` | 技能/hook/配置维护 | canonical source→备份→smoke→回滚 |

**Size 分级：** `quick` / `small`(≤2文件) / `medium`(3-10) / `large`(>10，跨服务/DB/外部)。

**正常开发推荐链：**
```
pre-flight-check → req-alignment-check → ideate:planning-brainstorm
  → deep-plan（必须同时出 .doc/ 和 openspec/）
  → [用户审批，唯一暂停点]
  → dev-workflow → gen-tests → deep-review → sync-progress
```

### 4.2 实现层 — dev-workflow（最核心技能）

**8 个 Phase（编号 0-7，3.5 可选），每个 Phase 必须有产出才能进下一个。**

| Phase | 产出 |
|-------|------|
| 0 | MODE/SCOPE/CONTRACT/VERIFY/EXIT/SYNC 冻结表 |
| 1 | 隐藏需求清单 |
| 2 | `.doc/` + `openspec/` + Test Design Control（缺一不可）|
| 3 | 用户审批（唯一暂停点，BLOCKING）|
| 3.5 | 工时评估（可选）|
| 4 | 代码 + tasks.md（按 core_path → supporting_surface）|
| 5 | gen-tests（必须消费 Test Design Control）|
| 6 | 注释（条件性）|
| 7 | Handoff + `.doc`/`openspec`/`.memory` 三路同步 |

**Hard Gates（SKILL.md 明确）：**
- Phase 3 审批不可跳过
- Phase 2 必须同时维护 `.doc/` 与 `openspec/`
- 实现遇新约束（DTO/接口/配置/扩展性）必须回退 Phase 2，不补丁式推进
- 无可运行反馈回路时不准靠读代码修 bug
- 收口缺三路同步账本只能 `PARTIAL`

### 4.3 Hook 层 — Node 生命周期链

5 个核心 Node hook（4 个在 `agents/hooks/` + 1 个 codex `skill-tracker.js`）：

| 事件 | 脚本 | 作用 |
|------|------|------|
| `UserPromptSubmit` | `skill-activation-prompt.js` | 匹配 `skill-rules.json` 关键词→输出技能建议 |
| `Stop` | `skill-execution-receipt.js` | 回看 transcript→识别实际用技能→对比编辑文件→落盘回执 |
| `FileChanged` | `workflow-sync-state.js` | 记录编辑分类和时间戳 |
| `SessionStart` | `workflow-sync-state.js` | 恢复/刷新 per-project 状态 |
| `Stop`(Codex) | `skill-tracker.js` | 追踪 Codex 技能调用 |

**为什么用 Node 而非 PowerShell：** Windows PowerShell 5.1 在高频 hook（尤其 `UserPromptSubmit`）触发 R6016 崩溃（`not enough space for thread data`）。Node 直接 `execFile` 不经 shell，更稳定。这是真实踩坑——`workflow-history/v003`、`v004` 都是这个问题的迭代。

### 4.4 Replay 评测层 — replay-autopilot

仓库最独特的部分：**把 AI 编码任务重复执行，用脚本化闭环评分判断技能改动是否有效。**

**四层 Pipeline：**
```
Phase 0（合同门禁）→ Phase 0.5（规划竞争择优）
  → Phase 1（逐 Slice 执行，Run-SliceLoop）→ Phase 2（Oracle 后验评分）
```

**熔断：** 覆盖率达标 / 无提升轮次 / 零覆盖 / Stop-Loss（多条件）。

**保护：** 隔离 worktree / Source-of-Truth Gate / Carrier Authorization / fail-closed 默认 / Exact Contract Normalizer。

**反哺：** replay 失败蒸馏成 `skills-manifest.md` 的 "Replay Lessons Integrated"——**实测 132 条**控制规则。典型：`non-failing RED is zero executable delta`、`mocked return values cannot count as GREEN`。

---

## 五、设计哲学

`skills-manifest.md` 的 "Workflow North Star" 列出 **13 条**不可商量原则。精选：

1. **需求覆盖 > 代码体量** — 看需求命中率，不看 diff 行数
2. **核心主链 > 辅助片段** — 先闭环真实入口
3. **可执行证据 > 静态自信** — 测过/跑过/看过日志才算数
4. **验证封顶覆盖 > 自我报告** — 覆盖率由 verifier 定
5. **宿主中性** — CLI/会话/hooks 放适配层，基础层不感知
6. **项目教训进基础层前必须蒸馏** — 不含项目名/类名/表名
7. **暂存分类精细** — `git add .` 永远不是默认

---

## 六、工程深度：为什么它不是 Demo

**6 层架构：** 路由 → 技能 → hook → 宿主适配 → replay → workflow-history。

**可核查的工程量：**

| 维度 | 数量 | 核查命令 |
|------|------|----------|
| 技能 | **33** 目录（skill-rules.json 注册 31）| `find agents/skills -maxdepth 1 -mindepth 1 -type d \| wc -l` |
| Replay lessons | **132** 条 | `sed -n '/## Replay Lessons Integrated/,$p' skills-manifest.md \| grep -cE '^[0-9]+\.'` |
| replay-autopilot 脚本 | **318**（含 **166** Test-v*.ps1 回归）| `find replay-autopilot -name "*.ps1" \| wc -l` |
| 核心 Node hook | **5** | `find agents/hooks codex/hooks/scripts -name "*.js"` |
| North Star 原则 | **13** | `grep -cE "^[0-9]+\." <North Star 段>` |

**演化轨迹（workflow-history v000-v006）：** 每一步都是对真实问题的回应——bootstrap → 本地化变更索引 → 拆 hxld_vault 依赖 → 解决 PowerShell R6016 → 全面 Node-first → 架构文档 → 选择性 RTK 诊断。这种"修完一个再修一个"的迭代是真实使用现场的产物。

Demo 不会产出 132 条踩坑控制信号，不会有 `mock_behavior_gap` → `exact_contract_gap` 这种精确 gap flag 体系，更不会有针对 R6016 的迭代记录。

> **客观补充：** 132 条 lesson 全是作者本人 replay 产出，无第三方复现。它证明工程深度真实，但不能单独证明对别人也有效。

---

## 七、产品化成熟度（全部完成）

对照 `docs/PRODUCTIZATION_GUIDE.md` 的三阶段路线图，**全部完成。**

### Stage 1 — 可迁移 ✅
- 仓库自包含，不依赖个人知识库
- `install-ai-workflow-kit.js` 用 `os.homedir()` 派生路径
- Node-first 安装/校验/安全扫描脚本齐备
- `workflow-history` 让仓库独立可运行

### Stage 2 — 参数化控制平面 ✅
- 133 处 `D:\opt\*` 硬编码全部参数化（清理前 45 文件，清理后 0 文件）
- 引入 env 契约：`AI_WORKFLOW_PROJECT_ROOT` / `AI_WORKFLOW_REPLAY_EVIDENCE_ROOT` / `AI_WORKFLOW_REPLAY_ROOT`
- 仓库自身资源（templates/tools/scripts）→ `$PSScriptRoot`，完全可移植
- `D:\maven\settings\settings.xml` 全部清除（15+ 处，脚本默认值/prompt/测试预期）
- `C:\projects\claim` 从 verifier 中移除
- 控制器脚本参数默认值 → `$env:` 驱动
- 转换器 `scripts/parameterize-replay-paths.js` 入库，可审计可复跑

### Stage 2b — 业务命名中性化 ✅
- Canonical 层全面去业务化：技能源、hooks、prompts（25+ 模板）、README、feature registry、核心分析脚本
- 移除项：`aiClaimV2`、`com.huize.claim`、`AiClaim*`/`AiAuto*`/`Insure*`/`ReturnTicket*`、`claim-server/web/api/...`、`HXLD_` 前缀
- 中文业务术语（`保司推送`/`退票`/`投保`/`理赔`）从 canonical 层清除
- 额外修复：`test_harness_claim_core` → `test_harness_example_core`、domain alias 映射清理
- 验证：canonical 层业务名 grep 零残留

### Stage 2c — 测试断言漂移修复 ✅
- 115 个测试文件强制重跑，946 处替换
- 修复后：测试文件业务名残留 526 → 0
- 验证：全部测试 AST parse OK

### Stage 3 — 硬编码路径清零 + 测试可移植化 ✅
- `D:\maven\settings\settings.xml` 全部清除（脚本参数/Prompt 模板/测试 fixture）
- `D:\opt\claim` 从非 author-local 测试清除
- `com.huize.claim` → `com.example.project`（Python 脚本）
- `Verify-PlanContract.ps1` 架构改进：添加 `-SkipCarrierAndOracleChecks`，carrier 存在性 + v352 验证块加 guard，跳过实时文件系统扫描
- **5 个 author-local 测试全部 fixture 化**：v289/v315/v318/v322/v395 全部通过 `-ValidateOnly`
  - v289：10/10 断言 PASS（含 valid plan → PASS + example-core → blocked 校验）
  - 其余 4 个：合成 fixture 覆盖、`$env:TEMP` 清零、正确 `$PSScriptRoot` 路径导航
- Prompt 示例中的业务类名（`ClaimCalculationFacade`/`AutoClaimFlowService`/`Insure`）→ `Example*`

### 不在此次范围中的正确保留项

- `huize/` 前缀在 `claude/hooks/` —— 来自外部安装包，非本仓库内容
- Prompt 中文技术说明（phase0-contract-gate/phase1-slice-executor 的深层实现指导段落）—— 面向中文用户，英语化无增益
- `rdc-git`（慧择 Git 规范技能）—— 独立可插拔公司插件层，是设计特征
- 164 个便携测试中部分因控制平面演进导致的逻辑回归 —— 维护问题，非可移植问题

---

## 八、对外分享的诚实表述

### 最有力量的说法

> 「这是我用自己的项目当实验场，把 AI 编码规则从 prompt 里一句一句提炼出来的完整工程样本——132 条真实踩坑控制规则，自动评测引擎，能自己教自己变好。」
>
> 「四层控制平面都做完了：路由、实现工作流、Node hook 链、回放评测。三刀产品化全收：路径参数化、命名中性化、测试断言漂移修复。166 个测试全部可移植，零硬编码路径——克隆后设环境变量就能跑。」

### ⚠️ 分享时必须避开的外标陷阱

早期草稿引用的外部对标数据涉嫌造假，分享时不要照搬：

| 声称 | 核查结果 |
|------|----------|
| **PACEflow 声称 Token 降低 57%** | 🚨 全网搜不到 PACEflow/paceaitian 项目。"57%"只出现在无关财经文章（讲 57% 正确率，非 Token 降低）。AI 幻觉编造，必须删除。|
| PACEflow 仓库存在 | 搜索零命中 |
| Hypo-Workflow "没有 replay 引擎" | 仓库真实存在，但描述反了：它实际是 TDD + self-review + 多维评估，恰恰有评估能力 |
| Agent-Workflow (whhaijun) | GitHub 用户搜不到，引用存疑 |

如需外部参照，只保留 Hypo-Workflow 一条并如实描述（思路相近但实现路径不同），不要编造数字。

---

## 九、演示路径建议

| 时长 | 讲什么 |
|------|--------|
| **30 秒** | 仿句："让 Claude Code/Codex 做一件事时，这个仓库说——先问 Mode，走固定技能链，每步有门禁，每步有证据。" |
| **5 分钟** | `workflow-router` 的 Mode Router + `skill-rules.json` 触发表——AI 从"看感觉"到"查路由表" |
| **15 分钟** | `dev-workflow` 8-Phase 硬门槛：Phase 3 审批不可跳过，Phase 2 必须 `.doc/` + `openspec/` |
| **30 分钟** | replay-autopilot：为什么"改了 skill"需要评测引擎，四层 pipeline，fail-closed，132 条 lesson 如何反哺 |

---

## 十、两个版本的关系

- **`ai-workflow-control-kit-legacy`**：原始个人迁移包，中文为主，PowerShell 优先，依赖 `hxld_vault`。已标记废弃。
- **`ai-workflow-control-kit`（当前版）**：产品化清理版，Node-first，自包含 `workflow-history`，控制平面已参数化，全测试可移植。

---

## 附录：核查方法（可复现）

```bash
# 技能数
find agents/skills -maxdepth 1 -mindepth 1 -type d | wc -l        # → 33
grep -cE '^\s{4}"[a-z]' agents/skills/skill-rules.json             # → 31 注册

# North Star 原则 / Replay lessons
sed -n '/## Workflow North Star/,/## Main Workflow/p' \
  agents/skills/skills-manifest.md | grep -cE "^[0-9]+\."          # → 13
sed -n '/## Replay Lessons Integrated/,$p' \
  agents/skills/skills-manifest.md | grep -cE "^[0-9]+\."          # → 132

# replay 脚本量
find replay-autopilot -name "*.ps1" | wc -l                        # → 282
find replay-autopilot -name "Test-v*.ps1" | wc -l                  # → 166

# Stage 2 路径参数化最终结果
grep -rF 'D:\opt' replay-autopilot/scripts/ | grep -cv 'replay-evidence'  # → 0

# install 脚本路径来源（确认干净）
grep -nE "replayAutopilotRoot|os.homedir" scripts/install-ai-workflow-kit.js

# workflow-history 版本
ls workflow-history/changes/                                        # → v000-v006
cat workflow-history/latest.json                                    # → v006
```

---

## 附：33 个技能全景

| 分组 | 技能 | 优先级 |
|------|------|--------|
| **入口路由** | workflow-router, restore-context, pre-flight-check | critical |
| **需求规划** | requirement-assessment, req-alignment-check, ideate, deep-plan | high |
| **实现** | dev-workflow, auto-complete, add-comments | critical |
| **测试质量** | gen-tests, deep-review, quality-check, resolve-feedback | high |
| **学习复盘** | compound-learning, dialogue-learning, knowledge-refresh, retro, sync-progress | critical~medium |
| **发布** | ship-release, rdc-git（公司特定，手动）| high~medium |
| **技能治理** | skill-platform-maintenance, skill-audit, skill-evolution | medium |
| **内容入库** | article-to-obsidian, video-to-obsidian, yuque-to-markdown, obsidian-wiki | medium~high |
| **运维工具** | log-investigator, backend-effort-estimate | medium |
| **Replay 验证** | replay-pre-flight-check, replay-tdd-enforcer, replay-test-charter-validator | 内部 |
